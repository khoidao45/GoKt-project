Update Todos

Create OutboxStatus enum and OutboxEvent entity in Domain layer

Create IOutboxRepository interface in Application layer

Add PublishRawAsync to IEventPublisher + implement in KafkaEventPublisher

Add OutboxEvent EF configuration + DbSet to AppDbContext

Implement OutboxRepository

Implement OutboxProcessor background service

Modify CreateRideRequestCommand to write to outbox instead of direct Kafka

Register outbox services in DependencyInjection.cs

Run EF migration for OutboxEvents table

Build and verify zero errors
Everything is implemented. Here is a complete breakdown of what was built and why each decision was made.

What was implemented
Folder structure

src/Gokt.Domain/
  Enums/OutboxStatus.cs                         ← Pending / Processed / Failed
  Entities/OutboxEvent.cs                       ← Rich aggregate with state machine

src/Gokt.Application/
  Interfaces/IOutboxRepository.cs               ← Application-layer contract
  Interfaces/IEventPublisher.cs                 ← +PublishRawAsync() overload
  Commands/Rides/CreateRideRequestCommand.cs    ← MODIFIED — writes to outbox

src/Gokt.Infrastructure/
  Persistence/
    AppDbContext.cs                             ← +DbSet<OutboxEvent>
    AppDbContextFactory.cs                      ← Design-time factory for migrations
    Configurations/OutboxEventConfiguration.cs  ← EF mapping + composite index
    Migrations/20260405032939_AddOutboxEvents   ← Generated migration
  Repositories/OutboxRepository.cs              ← FOR UPDATE SKIP LOCKED
  Messaging/KafkaEventPublisher.cs              ← +PublishRawAsync, idempotent producer
  BackgroundServices/OutboxProcessor.cs         ← Polling worker
Sequence diagram

HTTP POST /rides/request
│
├─ CreateRideRequestCommandHandler
│   ├─ RideRequest.Create(...)              ┐
│   ├─ request.StartSearching()             │  One EF SaveChanges
│   └─ OutboxEvent.Create(                 │  = one atomic PostgreSQL transaction
│         type:       "ride.requested",    │
│         messageKey: rideRequest.Id,      │
│         payload:    JSON)                ┘
│
└─ HTTP 201 response immediately returned

                   (every 2 seconds)
OutboxProcessor
│
├─ BEGIN TRANSACTION
├─ SELECT * FROM OutboxEvents
│    WHERE Status='Pending'
│    ORDER BY CreatedAt
│    LIMIT 20
│    FOR UPDATE SKIP LOCKED        ← multiple instances safe
│
├─ For each event:
│   ├─ KafkaPublisher.PublishRawAsync(type, key, payload)
│   │   ├─ success → event.MarkProcessed()
│   │   └─ fail    → event.IncrementRetry()
│   │                  if RetryCount >= 5 → event.MarkFailed()
│
├─ db.SaveChangesAsync()
└─ COMMIT TRANSACTION

                   (Kafka is up)
MatchingWorker / KafkaMatchingConsumer
│
└─ Consume "ride.requested"
    └─ IMatchingService.StartMatchingAsync(rideRequestId)
        └─ AutoMatchingStrategy → wave-based driver matching
Key design decisions
Decision	Reason
Same SaveChangesAsync for ride + outbox	Guarantees atomicity — if DB commit fails, no orphan Kafka message is produced
FOR UPDATE SKIP LOCKED	Multiple OutboxProcessor instances (API + MatchingWorker) never process the same row
MessageKey stored on OutboxEvent	Kafka partition routing is stable on replay; the consumer sees the same key on retry
PublishRawAsync	Payload is already serialized when stored; avoids double-serialization
EnableIdempotence = true on producer	Kafka producer-side deduplication within a session (network retries don't produce duplicates)
LastError column on OutboxEvent	Failed events are self-documenting in the DB — no grep-through-logs needed
IDesignTimeDbContextFactory	Migrations work without needing Redis/Kafka running
Max 5 retries → Failed status	Events that exceed retries are visible for alerting rather than silently dropped
Test scenarios
Scenario 1 — Kafka is DOWN:


-- Customer creates a ride request
-- DB state immediately after POST:
SELECT Id, Type, Status, RetryCount FROM "OutboxEvents";
-- → 1 row, Status='Pending'  ✓ event is NOT lost
Scenario 2 — Kafka comes back UP:


OutboxProcessor polls → finds Pending event
→ PublishRawAsync succeeds
→ Status = 'Processed', ProcessedAt = now
→ MatchingWorker receives the message
Scenario 3 — Persistent Kafka failure (5+ retries):


SELECT Id, Status, RetryCount, LastError FROM "OutboxEvents"
WHERE Status = 'Failed';
-- → rows visible for alerting / manual replay
Scenario 4 — Two OutboxProcessor instances race:


Instance A: SELECT … FOR UPDATE SKIP LOCKED → locks rows 1-20
Instance B: SELECT … FOR UPDATE SKIP LOCKED → gets rows 21-40 (skips locked)
No duplicate processing ✓
c:\Users\MINH KHOI\source\repos\Gokt\README.md
1. THIẾU DEAD LETTER / REPLAY

Hiện tại:

Retry > 5 → Failed

👉 OK… nhưng:

❌ rồi sao nữa?
Show less
Good catch. Let me look at the current state before implementing:

Read c:\Users\MINH KHOI\source\repos\Gokt\src\Gokt.Infrastructure\BackgroundServices\OutboxProcessor.cs
Read c:\Users\MINH KHOI\source\repos\Gokt\src\Gokt.Application\Interfaces\IOutboxRepository.cs
Globpattern: "Gokt/Controllers/**/*.cs"
Found 6 files
Read c:\Users\MINH KHOI\source\repos\Gokt\src\Gokt.Domain\Entities\OutboxEvent.cs
Read c:\Users\MINH KHOI\source\repos\Gokt\Gokt\Controllers\RidesController.cs
Three things needed: DLQ publish, replay command, and admin endpoint. Let me implement all three:

Update Todos

Add ResetForReplay() to OutboxEvent entity

Extend IOutboxRepository with GetFailedBatchAsync + ReplayAsync

Publish to DLQ topic in OutboxProcessor when max retries exceeded

Create OutboxEventDto + GetFailedOutboxQuery + ReplayOutboxCommand

Implement OutboxRepository new methods

Create OutboxController admin endpoint

Build and verify
Step 1 — add ResetForReplay() to the domain entity:

Edit c:\Users\MINH KHOI\source\repos\Gokt\src\Gokt.Domain\Entities\OutboxEvent.cs
Added 12 lines
Step 2 — extend IOutboxRepository:

Write c:\Users\MINH KHOI\source\repos\Gokt\src\Gokt.Application\Interfaces\IOutboxRepository.cs
31 lines
Update Todos

Add ResetForReplay() to OutboxEvent entity

Extend IOutboxRepository with GetFailedBatchAsync + ReplayAsync

Publish to DLQ topic in OutboxProcessor when max retries exceeded

Create OutboxEventDto + GetFailedOutboxQuery + ReplayOutboxCommand

Implement OutboxRepository new methods

Create OutboxController admin endpoint

Build and verify
Step 3 — DLQ publish in OutboxProcessor:

Write c:\Users\MINH KHOI\source\repos\Gokt\src\Gokt.Infrastructure\BackgroundServices\OutboxProcessor.cs
175 lines
You've hit your limit · resets 2pm (Asia/Saigon)